components:
  schemas:
    Content:
      type: object
      description: A content
      required:
        - characters
      properties:
        characters:
          description: A character list
          type: array
          items:
            $ref: "./character.yaml#/components/schemas/Character"
